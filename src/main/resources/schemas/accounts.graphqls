schema {
  query: Query
}

type Query {
  account(id: ID!): Account
  accounts: [Account]
}

type Account {
  firstName: String
  lastName: String
  address: String
  phone: String
  website: String
  company: String
  accountType: String
}
